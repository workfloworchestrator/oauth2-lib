name: Test python package

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13', '3.14']
        resolution: ['locked']
        include:
          - python-version: '3.11'
            resolution: 'lowest-direct'
          - python-version: '3.14'
            resolution: 'highest'
      fail-fast: false
    steps:
      - uses: actions/checkout@v6
      - name: Install uv and set the Python version
        uses: astral-sh/setup-uv@v9.0.0
        with:
          version: "0.12.3"
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          case "${{ matrix.resolution }}" in
            locked)        uv sync --locked --group test --group dev ;;
            lowest-direct) uv sync --resolution lowest-direct --upgrade --group test --group dev ;;
            highest)       uv sync --resolution highest --upgrade --group test --group dev ;;
            *)             echo "::error::unknown resolution '${{ matrix.resolution }}'" && exit 1 ;;
          esac
      - name: Mypy
        run: uv run mypy .
      - name: License headers
        run: |
          uv run apache-license-check --copyright "2019-`date +%Y` SURF" oauth2_lib
      - name: Run pre-commit hooks
        run: uv run pre-commit run --all-files
      - name: Test with pytest
        run: |
          mkdir reports
          uv run pytest -vvv --cov-branch --cov-fail-under=80 --cov=oauth2_lib --cov-report=xml
#  coverage-combine:
#    needs: [build]
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#      - uses: actions/setup-python@v5
#        with:
#          python-version: '3.8'
#      - name: Get coverage files
#        uses: actions/download-artifact@v4
#        with:
#          name: reports
#          path: reports
#      - run: pip install coverage[toml]
#      - run: ls -la reports
#      - run: coverage combine reports
#      - run: coverage report
#      - run: coverage xml
#      - name: "Upload coverage to Codecov"
#        uses: codecov/codecov-action@v3
#        with:
#          token: ${{ secrets.CODECOV_TOKEN }}
#          fail_ci_if_error: true
#          files: ./coverage.xml
